# 📚 Networking Notes

## AWS Networking Services
- **Amazon VPC (Virtual Private Cloud)** → Isolated network for resources.  
- **Subnets** → Divide VPC into public/private networks.  
- **Route Tables** → Define traffic flow rules.  
- **Internet Gateway (IGW)** → Enables internet access.  
- **NAT Gateway** → Allows private instances to access the internet.  
- **Security Groups & NACLs** → Control inbound/outbound traffic.  
- **Amazon Route 53** → DNS service.

## Key Concepts
- CIDR blocks for IP address ranges.  
- Public vs Private subnets.  
- High availability across multiple AZs.  
- Shared responsibility in securing networks.


# 🛠 Networking Labs

## Lab 1: Create a VPC
1. Open VPC console → Create VPC.
2. Define CIDR block (10.0.0.0/16).
3. Create public and private subnets.
4. Attach an Internet Gateway.
5. Configure route tables.

## Lab 2: Launch an EC2 in a VPC
1. Create a new EC2 instance.
2. Place it in the public subnet.
3. Assign Elastic IP.
4. Test connectivity via SSH.
