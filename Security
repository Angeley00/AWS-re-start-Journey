# 📚 Security Notes

## AWS Security Services
- **IAM (Identity and Access Management)** → Users, groups, roles, policies.  
- **AWS KMS (Key Management Service)** → Encrypt data with managed keys.  
- **AWS Shield** → DDoS protection.  
- **AWS WAF** → Web Application Firewall.  
- **CloudTrail** → Logs all API calls.  
- **GuardDuty** → Threat detection.  

## Key Concepts
- Principle of Least Privilege.  
- MFA for accounts.  
- Shared Responsibility Model.  
- Security groups vs NACLs.  

# 🛠 Security Labs

## Lab 1: IAM User & Policy
1. Create an IAM user with programmatic access.
2. Attach a custom policy allowing S3 access.
3. Test login with new credentials.

## Lab 2: Enable MFA
1. Go to IAM → Security credentials.
2. Enable MFA for root user.
3. Configure with authenticator app.
